# Changelog

Soul's Travel 所有版本的变更记录，格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。

---

## [Unreleased]

## [0.1.0-004] - 2026-04-24 - **回程日景点与多段交通被整段漏抓**：像最后一天只在新宿吃一顿纪念餐、中途转乘多段火车/飞机的回程日，此前景点列、交通

> Theme: PDF 每日行程与费用分类准确性提升

### Fixed

- **回程日景点与多段交通被整段漏抓**：像最后一天只在新宿吃一顿纪念餐、中途转乘多段火车/飞机的回程日，此前景点列、交通段全部识别为空，日期摘要直接兜底成「Day N」；现在纯英文店铺名能被识别为景点，多种箭头字形下的城市转运链也能完整抽取，回程日也能看到完整的景点与交通。

- **五字长城市名被截断**：像「富士河口湖」这类五字城市此前在交通链里会被截成后三字「河口湖」；现在交通段完整保留原城市名，不再把出发地写错。

- **关 Terminal 后后端孤立、PDF 上传报 Network Error**：双击启动脚本后，如果直接关闭 Terminal 窗口而不是 Ctrl+C 退出，前端开发服务会被系统清理掉，但后端 Flask 进程会被遗留在 5002 端口；下次打开浏览器访问 `localhost:5000` 时页面虽然能从旧标签载入，但所有 PDF/图片/文字解析都会弹出「解析失败: Network Error」。现在启动脚本在关窗、挂起、脚本异常退出的场景下也会正确停掉后端，关窗后留下干净的状态，重启启动器即可恢复正常。

- **日本 IC 卡与电车票归类错误**：SUICA/PASMO 充值与 JR 电车票此前因关键字不全一律落到「其他」；现在日本常用交通 IC 卡和 JR、新干线、各类电车/地铁品牌能自动归入「交通」。

- **英文命名酒店归类错误**：像 Agora Place 东京浅草这类名字里没有「酒店/宾馆」字样的酒店此前会被扔进「其他」；现在会读取 PDF 详情页的「住宿」类型标签，把它们正确落到「住宿」。

- **「旅店」后缀酒店未识别**：描述带「旅店」后缀的住宿（如湘南藤泽阿蒙特旅店）此前未被识别为酒店；现在「旅店」与「旅馆」同等视为住宿。

- **观景/瞭望景点归类错误**：Sunshine60 瞭望台等详情页标注「其他景点」的观景项此前落到「其他」；现在会正确归入「门票」。

- **居酒屋/餐厅被误判为酒店**：鸟贵族、MO-MO-PARADISE 等餐厅因「当日没有住宿消费」被自动填成当日住宿，污染行程里的住宿字段；现在只在当日有且仅有一条无法判定的「其他」或描述明确含酒店关键字时才做反填，餐厅消费保留原分类、回程日住宿保持为空。

## [0.1.0-003] - 2026-04-23 - **本地图片行程识别**：上传手账风格的行程截图（"N 日游"标题 + 每日日期分段 + 景点清单）后，无需任何云端密钥

> Theme: 统计页改造成"旅行生涯叙事"

### Added

- **本地图片行程识别**：上传手账风格的行程截图（"N 日游"标题 + 每日日期分段 + 景点清单）后，无需任何云端密钥即可直接解析出标题、起止日期、每日景点与酒店，并按城市自动拆分行程段；此前上传图片会因缺少识别配置直接报错。

- **世界足迹地图**：统计页新增可交互的世界地图，去过的国家按访问次数渐变着色，鼠标悬停看次数。

- **旅行之最卡片**：一眼看到最早、最长、最贵、最省、评分最高、评分最低六次旅行，每张卡自带目的地与行程标题。

- **大洲画像**：把访问过的国家按大洲聚合，看到亚洲、欧洲、北美洲等分布的国家数与出行次数。

- **月份出行分布与旅伴画像**：新增按月出行次数条形图（春夏秋冬分色），以及独行/结伴/家庭三类旅伴占比饼图。

- **各国花费榜**：新增按国家聚合的花费排行，直观看到钱主要花在哪些目的地。

- **解锁地球百分比**：总览新增"解锁地球 X%"数字卡，把已访问国家数放到全球国家总数里对比，旅行成就感更强。

### Changed

- **世界足迹左右分栏**：地图与大洲卡片由上下分布改为左右分栏，地图在左、大洲卡片在右单列堆叠，地图高度随大洲卡片总高度自适应，不再无谓挤占首屏。
- **统计页重构为生涯叙事**：由原先的"图表拼盘"改为"从 YYYY 年出发，已走过 N 次远方"开篇的分节叙事，依次呈现世界足迹、旅行之最、时间画像、同行画像与开销洞察五个章节。

### Fixed

- **更多行程开销自动归类**：英国、新加坡、韩国等行程 PDF 里，描述直接写作「美食」「活动」「城堡」「博物馆」「动物园」「游艇」以及「GOCITY / GO CITY / Luge」等城市通票/门票型开销常常因关键字不全而被一律扔进「其他」；现在这类条目会自动落入餐饮或门票，「其他」类仅保留真正无法判定的项（如保险、签证、现金/信用卡汇总）。

- **后传文件清空已导入费用**：同一个新建页先上传 PDF、再上传 JPG 覆盖行程时，PDF 已解析出的费用明细会被 JPG 的空费用列表清掉；现在后传的行程模板只在有内容时替换原值，空字段自动保留已导入数据，费用不再蒸发。

- **行程节奏只看数量不看难度**：原先"非交通日平均每天 N 个活动"只数数量，把 8 个夜市/广场/餐厅 和 8 个博物馆/自然保护区/徒步一视同仁；现在会按活动难度加权——夜市/集市/广场/游轮/餐厅等轻量项算 0.5 份，博物馆/古城/自然保护区/登山/潜水等耗时体力项算 1.5 份，其余按 1 份。最忙的一天也按加权活动量而不是条目数识别，不再把轻松逛吃的"多条目日"误判为"偏满"。

- **景点费用错放"其他"类**：东南亚行程 PDF 中，博物馆、集市、动物园、生态公园、夜游船等景点/活动因名字带"中文+英文"双语常被误归"其他"；现在这类景点票务支出能按类型标签正确落到"门票"，"其他"类仅保留真正通用的现金/信用卡汇总。

## [0.1.0-002] - 2026-04-22 - PDF 导入健壮性提升与多模态录入

### Fixed

- **长行程跨页漏抓天数**：穷游 PDF 中十几天的行程跨多页时，之前会在分页处断开、后面几天整段丢失；现在跨页行程能完整保留每一天。

- **¥0 占位景点混入花费明细**：未填价格的景点会以「¥0」占位，之前被当作真实支出录入，现在花费明细只保留有金额的项目。

- **景点名里的数字字母被截断**：像「圣淘沙 4D 探险乐园」之前只剩「圣淘沙」，名字里的数字、字母、版本号组合现在都能完整保留。

- **长描述开头缺字**：中英混排的景点长描述之前开头几个字会被吃掉（如「新加坡夜间野生动物园,Night Safari of Singa」变成「坡夜间野生动物园…」），现在完整显示。

- **历史酒店被当酒店过滤掉**：像莱佛士酒店这类会被当作景点参观的历史建筑之前被误过滤，现在正确出现在当日景点中。

- **马尔代夫等目的地识别不到**：马尔代夫、马富施等海岛目的地之前会落到「待确认」分组，现在能正确聚合成独立的行程段。

- **住宿名被费用表里的简写覆盖**：概览抽到的完整酒店名（如「竞技场海滩酒店」）不再被费用表里的简写（「马尔代夫竞技海滩酒店」）替换。

- **日期与星期两种排版混排时漏抓**：同一份 PDF 里日期和星期的排版有两种写法并存时，之前会漏掉部分日子的景点、住宿与交通，现在两种格式都能正确识别。

### Added

- **截图与照片识别行程**：支持把行程截图或照片直接投到对话区，AI 会自动识别日期、地点、住宿等信息并填入行程。

- **穷游链接一键导入**：粘贴穷游计划页链接即可自动抓取并拆分成结构化行程，不再需要先下载 PDF。

- **一次消息批量下达多条指令**：在对话里用分号串多条指令（如「加一笔晚餐花费；把第二天改名为…」），AI 会按顺序逐条应用到行程上。

- **多币种金额自动换算**：记账时直接写原币金额（美元、日元、欧元、港币等十种常见货币），系统会按固定汇率折算成人民币汇总。

- **大交通自动从本地交通里拆出**：行程中的跨城航班、高铁会被单独识别，不再和打车、地铁混在一起统计，花费口径更清晰。

- **短指令意图识别更稳**：对「加一笔…」「改成…」「删掉…」之类的日常口语，AI 能更稳定地理解成对应操作，不会误解成纯文字备注。

### Changed

- **PDF 文字抽取更稳，乱码消失**：换用了更稳健的 PDF 解析管线，穷游 PDF 详情页之前偶发的重复字符与特殊符号乱码不再出现。

- **AI 解析入口统一**：长段文字和短指令合并到同一个对话入口，使用时无需再区分「粘贴行程」还是「发指令」。

- **评价更贴合当地物价**：行程评价新增多个亚洲城市的消费基准与季节因子，花费合理性判断更接近当地实际水平。

- **详情、统计与编辑界面细节打磨**：行程卡片、每日时间轴、开销表、统计图表、评价卡片等关键界面做了一轮视觉与交互优化，整体更顺手也更轻盈。

- **项目文档全面更新**：架构说明、设计文档与使用指南同步到当前实现，新上手的协作者或 AI 协作时更少踩坑。

---

## [0.1.0] - 2026-04-11

### Added
- **行程全流程管理**：创建、编辑、归档与删除旅行计划，支持按航段拆分并为每日填入活动、住宿与开销，自动维护「计划中 / 进行中 / 已完成」三种状态。

- **结构化行程详情页**：以航段卡片加每日时间轴的方式组织日程，侧栏同步展示当段总开销，浏览和核对一目了然。

- **AI 对话式编辑器**：左侧聊天、右侧表单同屏协作编辑，可把粘贴的文字或文件自动拆成日程，同时保护你手动改动过的内容不被 AI 覆盖。

- **长文本与 PDF 导入**：把预订邮件、行程单或 PDF 直接投入对话区，自动识别目的地、日期、住宿、交通与花费并填入行程。

- **旅行统计面板**：一页看完到过几个国家、总花费、人均日消费等核心指标，并用饼图与柱图呈现开销结构和历次行程对比。

- **年度旅行时间轴**：按年份倒序把所有旅行串成一条垂直时间线，方便快速回顾某一年去过哪些地方。

- **只读分享链接**：一键为某次行程生成公开分享地址，对方不用登录即可查看完整日程，也不会看到你的其他数据。

- **行程质量评价**：从花费、节奏、住宿、交通、景点五个维度自动打出 0–100 分并给出综合结论，帮助你复盘一次旅行的体验与性价比。

- **个性化评分背景**：可补充收入区间、年度旅行预算与家庭情况，评价会据此调整花费口径和点评措辞，让打分更贴合自身水平。

- **访问足迹自动记录**：保存或更新行程时自动累计已到访的国家与城市，可在个人抽屉里随时查看与编辑。

- **景点补录建议**：评价页会列出可能遗漏的当地热门景点，点击即可选日期追加到行程，减少手动排日程的负担。

- **内置样例行程**：初次启动自带一条完整示例行程，便于快速熟悉界面与各项功能，不会影响之后自行创建的真实数据。

- **一键启动入口**：提供通用启动脚本与 macOS 双击启动器，同时拉起前后端服务，零命令行也能在本地跑起来。

- **清爽渐变界面**：大留白布局搭配天蓝主色的渐变卡片，整体视觉轻快，长列表浏览也不容易疲劳。

### Changed
- **个人资料入口前移**：把个人背景设置从行程详情页搬到了行程列表页右上角，新建行程前就能先完善资料，访问更顺手。

### Fixed
- **macOS 启动器兼容特殊路径**：修复目录名包含单引号时双击启动器无法运行的问题，项目放在带特殊字符的路径下也能正常启动。

- **本地访问地址稳定**：本地运行时前端始终使用固定入口地址，避免被其他服务抢占后自动跳转，收藏的链接不再失效。

- **个人资料字段回显一致**：修复个人抽屉里收入、预算、家庭情况几项偶发无法读取或保存的问题，填写后刷新仍能正确显示。

---

[Unreleased]: https://github.com/your-repo/souls-travel/compare/v0.1.0-002...HEAD
[0.1.0-002]: https://github.com/your-repo/souls-travel/compare/v0.1.0...v0.1.0-002
[0.1.0]: https://github.com/your-repo/souls-travel/releases/tag/v0.1.0
