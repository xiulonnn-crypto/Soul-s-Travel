# Changelog

Soul's Travel 所有版本的变更记录，格式遵循 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)。

---

## [Unreleased]

## [0.1.0-002] - 2026-04-22 - **PDF 概览跨页漏抓行程日**：概览表跨多个 PDF

### Fixed
- **PDF 概览跨页漏抓行程日**：概览表跨多个 PDF 页的行程（如 11 天行程横跨 P1/P2）之前会在第一个分页符处被截断，导致后续几天丢失；现在只在费用/预算表起始处截断，并在每日 chunk 内就地处理分页边界，跨页行程能完整呈现。
- **花费为 0 的景点行混入花费明细**：穷游 PDF 将未填价格的景点以「¥0」占位列出，这类条目不再进入花费明细，明细只保留真实支出。
- **景点名含数字时被截断**：例如「圣淘沙 4D 探险乐园」之前只抽到「圣淘沙」，现在名字中的数字字母组合（4D / 3.0 / Hep Five 等）能完整保留。
- **花费明细描述开头缺字**：跨语言长描述（例如「新加坡夜间野生动物园,Night Safari of Singa」）之前会被截断成「坡夜间野生动物园…」，现在完整显示。
- **已作为景点游览的历史酒店漏抓**：像「莱佛士酒店」这类用户真的去参观的历史建筑之前被误过滤，现在正确出现在当日景点中。
- **马尔代夫行程归入「待确认」城市**：马尔代夫、马富施作为目的地城市之前不被识别，现在能正确聚合为独立 leg。
- **住宿字段被同一家酒店的费用简写名覆盖**：概览表中抽到的完整中文酒店名（如「竞技场海滩酒店」）不再被费用表里的简写版本（「马尔代夫竞技海滩酒店」）覆盖。
- PDF 行程解析在同一文档中混合两种日期标记行格式时漏抓部分天数 —— 表格型布局下 pdfminer 会把"NN\n星期X"与"NN 星期X"混排输出，导致多日景点/酒店/交通合并或丢失，现已兼容两种格式

### Added
- NLU 短文本意图分类系统（jieba + TF-IDF + LinearSVC），支持 6 种意图：add_expense / change_category / set_accommodation / rename_activity / remove_expense / add_activity
- 图片行程解析（`image_parser.py`）：OpenAI 兼容 Vision API，解析截图/照片中的行程信息
- 穷游 URL 抓取（`url_scraper.py`）：Playwright 移动端 UA 抓取 `plan.qyer.com`，自动解析行程结构
- 文件内容提取工具（`file_extractor.py`）：统一处理 PDF、图片、文本文件的内容提取
- 解析管道支持复合指令：分号分隔多条 NLU 指令，返回 `type: compound` 批量操作
- 多货币费用解析：支持 CNY / USD / EUR / JPY / KRW / HKD / SGD / THB / MYR / TWD，按固定汇率换算
- 大交通识别策略（`_is_flight_expense`）：关键词匹配 → 路线模式 → 本地交通排除，剔出国际段后计算花费评分
- 后端测试套件扩充至 ~131 个测试函数，新增 Japan / Korea / Malaysia / 多 PDF 解析测试 + 评价系统测试
- `tests/conftest.py`：统一 DB 隔离 fixture（内存 SQLite），消除各文件重复定义
- `TECHNICAL.md`：完整技术文档（接口设计 / DB Schema / NLU 系统 / 测试规范 / Harness 工程规划 / 工程铁律 / 部署运维）

### Changed
- PDF 文本抽取改由 pymupdf4llm 作为主路径，pdfplumber 页 0 表格作为景点/日期锚点补充，原 pdfplumber+pdfminer 混合抽取保留为失败降级；穷游 PDF 中的详情页不再出现字符重复与 PUA 乱码
- `ai_parser.py` 重构：长文本规则解析 + 短文本 NLU 路由合并，`parse_text()` 统一入口
- `evaluator.py` 改进：增加城市基准价格、季节因子调整、大交通口径一致性修正
- `city_benchmarks.json` 扩充：新增多个亚洲目的地城市消费基准数据
- `models.py` 补充：对齐评价模块字段，完善 `to_dict()` 输出
- `routes/parse.py`、`stats.py`、`trips.py` 路由优化，补充错误处理与边界检查
- 前端组件整体 UI 细化：TripCard、TripEvaluation、TripForm、ChatPanel、StatsChart、ExpenseTable
- `Stats.jsx` 数据展示改进，`TripDetail.jsx`、`TripEditor.jsx` 交互体验优化
- 项目文档全面更新：ARCHITECTURE.md / CLAUDE.md / DESIGN.md / README.md

---

## [0.1.0] - 2026-04-11

### Added
- **行程全流程管理**：创建、编辑、归档与删除旅行计划，支持按航段拆分并为每日填入活动、住宿与开销，自动维护「计划中 / 进行中 / 已完成」三种状态。

- **结构化行程详情页**：以航段卡片加每日时间轴的方式组织日程，侧栏同步展示当段总开销，浏览和核对一目了然。

- **AI 对话式编辑器**：左侧聊天、右侧表单同屏协作编辑，可把粘贴的文字或文件自动拆成日程，同时保护你手动改动过的内容不被 AI 覆盖。

- **长文本与 PDF 导入**：把预订邮件、行程单或 PDF 直接投入对话区，自动识别目的地、日期、住宿、交通与花费并填入行程。

- **旅行统计面板**：一页看完到过几个国家、总花费、人均日消费等核心指标，并用饼图与柱图呈现开销结构和历次行程对比。

- **年度旅行时间轴**：按年份倒序把所有旅行串成一条垂直时间线，方便快速回顾某一年去过哪些地方。

- **只读分享链接**：一键为某次行程生成公开分享地址，对方不用登录即可查看完整日程，也不会看到你的其他数据。

- **行程质量评价**：从花费、节奏、住宿、交通、景点五个维度自动打出 0–100 分并给出综合结论，帮助你复盘一次旅行的体验与性价比。

- **个性化评分背景**：可补充收入区间、年度旅行预算与家庭情况，评价会据此调整花费口径和点评措辞，让打分更贴合自身水平。

- **访问足迹自动记录**：保存或更新行程时自动累计已到访的国家与城市，可在个人抽屉里随时查看与编辑。

- **景点补录建议**：评价页会列出可能遗漏的当地热门景点，点击即可选日期追加到行程，减少手动排日程的负担。

- **内置样例行程**：初次启动自带一条完整示例行程，便于快速熟悉界面与各项功能，不会影响之后自行创建的真实数据。

- **一键启动入口**：提供通用启动脚本与 macOS 双击启动器，同时拉起前后端服务，零命令行也能在本地跑起来。

- **清爽渐变界面**：大留白布局搭配天蓝主色的渐变卡片，整体视觉轻快，长列表浏览也不容易疲劳。

### Changed
- **个人资料入口前移**：把个人背景设置从行程详情页搬到了行程列表页右上角，新建行程前就能先完善资料，访问更顺手。

### Fixed
- **macOS 启动器兼容特殊路径**：修复目录名包含单引号时双击启动器无法运行的问题，项目放在带特殊字符的路径下也能正常启动。

- **本地访问地址稳定**：本地运行时前端始终使用固定入口地址，避免被其他服务抢占后自动跳转，收藏的链接不再失效。

- **个人资料字段回显一致**：修复个人抽屉里收入、预算、家庭情况几项偶发无法读取或保存的问题，填写后刷新仍能正确显示。

---

[Unreleased]: https://github.com/your-repo/souls-travel/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/your-repo/souls-travel/releases/tag/v0.1.0
